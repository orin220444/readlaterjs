const handleRandom = require('./random');
const handleAdd = require('./add');
const handleQuery = require('./query');
const handleMainten = require('./mainten');
module.exports = {
  handleRandom,
  handleAdd,
  handleQuery,
  handleMainten,
};
